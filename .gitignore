/target
.DS_Store
*.log
.env
.env.*
!.env.example

# Built Astro site. NOT committed — the deployment builds it (from
# canonical-frontend) and hands it to this backend via STATIC_DIR. For local
# dev, point STATIC_DIR at ../canonical-frontend/dist.
/static

# Scratch / git worktrees (tmp/worktrees/*)
tmp/
