#!/usr/bin/env sh
set -eu
cd "$(git rev-parse --show-toplevel)"
if ! git diff --cached --name-only --diff-filter=ACMR -- src/pages contracts conformance .zpkg.toml zed-env.toml .zed .githooks scripts | grep -q .; then exit 0; fi
sh conformance/check-routing-contracts.sh --structural-only
